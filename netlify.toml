[build]
  command = "npm run build"
  # next.config.mjs uses `output: "export"`, so the build writes plain
  # static HTML to `out/` — not `.next/`.
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
